{
  "name": "uikit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/*.pug --open",
    "build": "parcel build src/*.pug -d docs --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "pug": "^2.0.4",
    "sass": "^1.26.5"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
